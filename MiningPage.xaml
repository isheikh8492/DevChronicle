<Page x:Class="DevChronicle.MiningPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="Mining"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}">

    <ScrollViewer>
        <StackPanel Margin="56,24,56,24">
            <TextBlock
                FontSize="28"
                FontWeight="SemiBold"
                Text="Git Mining"
                Margin="0,0,0,8"/>
            <TextBlock
                FontSize="14"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                Text="Mine commits from Git repositories and organize by day."
                Margin="0,0,0,24"/>

            <ui:Card Margin="0,0,0,16">
                <StackPanel Margin="20">
                    <TextBlock FontSize="16" FontWeight="Medium" Text="Mining Controls" Margin="0,0,0,16"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,0">
                        <ui:Button
                            Content="Mine Last 14 Days"
                            Icon="{ui:SymbolIcon Database24}"
                            Appearance="Primary"
                            Margin="0,0,12,0"/>
                        <ui:Button
                            Content="Backfill Previous 30 Days"
                            Icon="{ui:SymbolIcon ArrowSync24}"
                            Margin="0,0,12,0"/>
                        <ui:Button
                            Content="Stop"
                            Icon="{ui:SymbolIcon Stop24}"
                            Appearance="Danger"/>
                    </StackPanel>
                </StackPanel>
            </ui:Card>

            <ui:Card>
                <StackPanel Margin="20">
                    <TextBlock FontSize="16" FontWeight="Medium" Text="Mining Progress" Margin="0,0,0,16"/>
                    <ProgressBar Value="0" IsIndeterminate="False" Height="4" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Days Mined"/>
                            <TextBlock FontSize="20" FontWeight="Medium" Text="0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Commits Found"/>
                            <TextBlock FontSize="20" FontWeight="Medium" Text="0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}" Text="Status"/>
                            <TextBlock FontSize="14" Text="Idle" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</Page>
